&nbsp;&nbsp;
<%= player_name(target_player, player) %>
<%= player_action(target_player, player, 'trash') %>
<%= show_cards(cards) %>
<%= "from #{source}" unless source.nil? %>
<%= "and #{player_action(target_player, player, 'discard')} the rest" unless discard.nil? %>
